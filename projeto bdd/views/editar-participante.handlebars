<style>
  body {
    font-family: monospace;
    margin: 0;
    padding: 0;
    background-color: #f9f9f9;
    background-image: url('');
    background-size: cover;  /* Faz a imagem cobrir toda a tela */
    background-position: center;  /* Centraliza a imagem */
    background-attachment: fixed;  /* A imagem fica fixa quando rolar a página */
  }

  header {
    background-color: #f572cedc;
    color: white;
    text-align: left;
    padding: 2px;
  }

  main {
    max-width: 1000px;
    margin: auto;
    margin-top: 20px;
    padding: 10px;
    background-color: #fff;
    border: 1px solid #ddd;
  }

  section {
    margin-bottom: 20px;
  }

  h1 {
    margin: 5px;
    margin-bottom: -10px;
    font-size: 70px;
    text-align: center;
    padding-top: 80px;
    padding-left: 30px;
    padding-right: 30px;
  }

  h2 {
    margin: 5px;
    margin-bottom: 5px;
    font-size: 40px;
    text-align: center;
    padding-top: 70x;
    padding-left: 30px;
    padding-right: 30px;
  }

  h5 {
    margin: 5px;
    margin-bottom: 5px;
    font-size: 20px;
    text-align: center;
    padding-top: 100x;
    padding-left: 30px;
    padding-right: 30px;
  }

  label {
    display: block;
    margin-bottom: 5px;
    font-weight: bold;
    margin-left: 180px;
  }

  input, select, button {
    padding: 8px; /* Isso adiciona um espaço dentro do campo de input */
    border: 1px solid #000; /* Apenas para ilustrar a borda do input */
    width: 1150px;
    margin-bottom: 10px;
    margin-left: 180px;
  }

  .container {
    flex-direction: column; /* Organiza os itens verticalmente */
    justify-content: center; /* Centraliza verticalmente */
    align-items: center; /* Centraliza horizontalmente */
    height: 100vh; /* Ocupa toda a altura da tela */
    margin: 0; /* Remove margens padrão */
    gap: 20px; /* Espaço entre os botões */
  }

  button {
    background-color: #f572cedc;
    color: white;
    font-family: monospace;
    font-size: 1rem;
    cursor: pointer;
    border-radius: 5px;
    margin-right: 100px;
    padding: 10px 20px;
    border: none;
    text-align: center;
    width: 1169px;  /* Define a largura do botão igual ao input */
  }

  button:hover {
    background-color: #f572ce65;
  }

  /* Estilização do pop-up */
.popup {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: white;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
  text-align: center;
  font-size: 18px;
}

.popup button {
  margin-top: 10px;
  padding: 8px 15px;
  border: none;
  background-color: #f572cedc;
  color: white;
  font-size: 16px;
  cursor: pointer;
  border-radius: 5px;
}

.popup button:hover {
  background-color: #f572ce65;
}

</style>

  <header>
    <h3> &#10036; Eventario </h3> <!-- Cabeçalho com o nome do sistema "Eventario" -->
  </header>
<body>
  <h1>Editar Participante</h1> <!-- Título da página para editar o participante -->

  <form action="/participantes/{{participante.id}}/editar" method="POST"> <!-- Formulário para editar um participante, usando o método POST -->
    <label for="nome">Nome:</label> <!-- Rótulo para o campo de nome -->
    <input type="text" id="nome" name="nome" value="{{participante.nome}}" required> <!-- Campo de input para o nome do participante, preenchido com o valor atual -->

    <label for="email">E-mail:</label> <!-- Rótulo para o campo de e-mail -->
    <input type="email" id="email" name="email" value="{{participante.email}}" required> <!-- Campo de input para o e-mail do participante, preenchido com o valor atual -->

    <button type="submit">Salvar Alterações</button> <!-- Botão para salvar as alterações no participante -->
  </form>

  <a href="/participantes"> <!-- Link para retornar à lista de participantes -->
  <button> Cancelar <!-- Botão de cancelar que redireciona para a lista de participantes -->
  </button></a>
</body>